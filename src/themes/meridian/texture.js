// texture.js — Meridian texture helpers (seeded dither optional)
// For full parity copy and retune from Nightwatch/Drydock texture.js
export function tex(opts) {
  // Minimal stub; real implementation generates cached data-URL dither
  return "none";
}
export function statementBg() { return "none"; }
