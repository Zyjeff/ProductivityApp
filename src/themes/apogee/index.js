// Apogee — ground control. The pad never sleeps.
// CRT console of a one-person orbital launch complex.

import App from "./app.jsx";
import css from "./tokens.css?inline";

export default {
  id: "apogee",
  name: "Apogee",
  tagline: "Ground control — the pad never sleeps.",
  App,
  css,
};
